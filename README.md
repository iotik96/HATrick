# HATrick(Hierarchical Attention for Transformers)🎩
## Он же алгоритм "Внимательной шляпы"!
# Что это и с чем это едят?
- Иерархический механизм внимания для обработки длинных последовательностей с использованием нескольких видов обычного self-attention и cls токенов для обобщения текстов, придуманный исследовательской командой "iotik96 team"(пока состоит из одного человека😮).
# Проблема
- Стандартный self-attention рассматривает все токены последовательности как равноправные и проводит вычисления взаимодействия абсолютно всех пар токенов, что приводит к:
  - квадратичной сложности
  - диффузии внимания
  - иерархия хоть и есть но в очень малой степени и не проявлется полноценно
# Идея:
Алгоритм состоит из нескольких этапов:
1. Разбиение последовательности на чанки одинаковой длины
2. В начало каждого чанка добавляется обучаемый CLS токен
3. **Локальное self-attention** применяется внутри каждого чанка(токены обогощаются вниманием только с токенами своего чанка)
4. **Глобальное self-attention** полученные CLS токены из каждого чанка объединяются и обогащаются вниманием между собой
5. **cross-self-attention** каждый токен обогащается вниманием только со всеми полученными CLS токенами, при этом сами CLS не изменяются
Для более подробного описания изучите документ **"Документации/Техническое описание.docx"**
# Теоретические преимущества
**⚠️⚠️⚠️В текущей версии(10.02.26)пока что преимущества являются теоретическими и требуют экспериментальной проверки**
1. Превосходящее качество при работе с огромными документами
2. Иерархическое понимание текста
Модель явно формирует:
  - локальные представления(на уровне чанков)
  - глобальные представления(на уровне CLS токенов)
Что как раз таки приближенно к структуре естественного языка(слова --> предложение --> текст)
3. Смещение под структурированные данные:
В механизм внимания заложено предположение что:
  - локальные зависимости важнее внутри чанков
  - от глобальных зависимостей доходит только наиболее важная и обощенная информация
4. Специализация токенов:
  - разделение на обычные токены эмбеддингов и специализированные cls токены для обобщения позволяет модели лучше понимать последовательность более структурированно
5. Вычислительная сложность:
  - Сжимается до O(Tn + (T/n)² + T²/n), где n - размер одного чанка
# Структура репозитория:
   1. modules - будут выкладываться актуальные версии программ
      - HATrick-class - разные версии базового блока алгоритма
   2. Документации - все теоретические подробности самого алгоритма
# Статус на данный момент:
- Проект находится на стадии активной разработки.
- Реализован класс внимания
- В ближайшее время уже планируются эксперименты с явными сравнениями с обычным self-attention

  
  
